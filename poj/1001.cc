#include <iomanip>
#include <iostream>
using namespace std;

int main(int argc, const char* argv[]) {
  int r, n;
  // while (cin >> r >> n) {
  // }
  cout << setiosflags(ios::skipws);
  cout << 0.0123 << endl;
  cout << true << endl;
  cout << false << endl;
  return 0;
}